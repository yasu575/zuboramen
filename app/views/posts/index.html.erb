<div class="container pt-3">
  <div class="row">
    <div class="col-lg-10 offset-lg-1">
      <div class="row">
        <div class="col-12">
          <div class="row">
            <% if @posts.present? %>
              <%= render @posts %>
            <% else %>
              <p><%= t('.no_result') %></p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

